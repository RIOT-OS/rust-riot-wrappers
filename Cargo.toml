[package]
name = "riot-wrappers"
version = "0.1.0"
authors = ["chrysn <chrysn@fsfe.org>"]

[dependencies]
embedded-hal = "0.2.0"
riot-sys = { path = "../riot-sys" }
crc = { version = "^1", default-features = false }

[features]
default = ["set_panic_handler"]
set_panic_handler = []
