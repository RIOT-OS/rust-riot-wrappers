// similar to board.h

#define I2C_DEV(x) irrelevant
#define I2C_UNDEF irrelevant
// FIXME this all means that riot-sys only works as long as i2c_t is not defined
// #define HAVE_I2C_T // but we don't
#define HAVE_I2C_SPEED_T // but we don't
#define HAVE_I2C_FLAGS_T // but we don't
